name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - numpy
  - eigen
  - pybind

    # Pip-only installs
  #- pip:
  #  - codecov

